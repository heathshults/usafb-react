import React from 'react';
// import PropTypes from 'prop-types';

const Filters = () => (
  <div className="filters">
    <h1>Filters</h1>
  </div>
);

Filters.propTypes = {

};

export default Filters;
